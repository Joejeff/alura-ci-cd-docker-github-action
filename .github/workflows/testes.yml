name: Explorer

on:
  workflow_dispatch:

jobs:

  Explorer:
    runs-on: ubuntu-latest
    #environment: dev

    steps:
    
    - name: Checkout
      uses: actions/checkout@v4.2.2

    - name: Echo ENVs  
      run: |
        #env
        pwd
        id
